/**
 * Reserved Pi package entry point.
 *
 * Runtime commands and tools will be registered here once the integration
 * layer is implemented. Keeping this entry point inert makes local package
 * loading safe during development.
 */
export default function registerWorkerGraph(): void {}
